# dev tooling (tsdown) requires Node 25.7+, but the published package supports
# Node 24+. The engines check on devDependencies would otherwise break local
# builds for Node 24 contributors. Build scripts enforce the Node 25.7+
# requirement themselves (see scripts/build-sea.mjs) and emit a clear error.
engine-strict=false
