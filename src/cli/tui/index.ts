// src/cli/tui/index.ts — entry point for the `umactually tui` subcommand.
// Body filled in by todo:9 (dispatch wiring) and todo:10 (hub menu).
// Stub exists so todos 9-14 can import the contract and run typecheck in parallel.
export type TuiExit = { exitCode: number };

export async function runTui(_argv: readonly string[]): Promise<TuiExit> {
  throw new Error("runTui: not yet implemented (filled in by todo:9)");
}
